import MaxWidthWrapper from "../maxWidthWrapper";
import BlogCard from "../blogCard";

export default function Tab() {
  return (
    <MaxWidthWrapper className="mt-6 mb-8">
      <BlogCard />
    </MaxWidthWrapper>
  );
}
